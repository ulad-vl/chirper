apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chirper-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: chirper-server
            port:
              number: 80